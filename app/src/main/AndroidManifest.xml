<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frontier.infinitescrollrecyclerview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InfiniteScrollRecyclerView">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.InfiniteScrollRecyclerView.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.GridHorizontalAutoScrollActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.InfiniteScrollRecyclerView.NoActionBar">
        </activity>
        <activity
            android:name=".activity.GridHorizontalHiveAutoScrollActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.InfiniteScrollRecyclerView.NoActionBar">
        </activity>
        <activity
            android:name=".activity.ListHorizontalAutoScrollActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.InfiniteScrollRecyclerView.NoActionBar">
        </activity>
    </application>

</manifest>